# TableFormatter

A utility class for rendering tabular data into formatted text tables, suitable for console output or log files. It supports both single collections and dictionaries, allowing customizable column widths and alignments.

## API

### `TableFormatter`

The default constructor initializes a new instance of the `TableFormatter` class with default formatting settings (left-aligned columns, no header separator).

### `Format`
