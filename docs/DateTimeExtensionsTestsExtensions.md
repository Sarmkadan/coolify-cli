# DateTimeExtensionsTestsExtensions

Provides a set of extension methods for `DateTime` objects, primarily used for testing scenarios in the `coolify-cli` project. These methods simplify date comparisons and calculations, such as checking if a date is in the past or future, computing business days between dates, and determining the first day of a quarter.

## API

### `IsInPast`
